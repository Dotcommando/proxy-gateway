{
  "name": "proxy-gateway-local-consumer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "npm run test:e2e",
    "smoke:types": "npm run test:types",
    "test:e2e": "npm run test:types && node --test --test-reporter=spec src/*.test.mjs src/*.test.cjs",
    "test:types": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@echospecter/proxy-gateway": "local"
  },
  "devDependencies": {
    "@types/node": "25.9.3",
    "typescript": "6.0.3"
  }
}
